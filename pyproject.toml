[project]
name = "vocaloid-title-search"
version = "0.1.0"
description = "Search well-known Vocaloid song titles by counted title length."
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "lxml>=6.1.1",
    "regex>=2026.5.9",
]
